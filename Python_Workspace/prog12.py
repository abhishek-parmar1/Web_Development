# create a new Virtual ENV = virtualenv < name of the env >

# Activate ENV = sorce <name of the env>/bin/activate
#(ENV ke andar jana)

# Install a library in the ENV = pip install <name of the library>
#(library ka nam ki spelling sahi honi chahiyae)

#Deactivate the ENV = deactivate
#(ENV se bahar ana)

#Google for spelling of the name of  the library

#Python package index = Jahan pe library ki spelling milti hai